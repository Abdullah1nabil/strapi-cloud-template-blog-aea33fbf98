'use strict';

/**
 * iraqi-app-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::iraqi-app-category.iraqi-app-category');
